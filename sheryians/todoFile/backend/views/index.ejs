<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo app wiht file</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/yourcode.js" crossorigin="anonymous"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block w-full rounded-md px-5 mt-2 py-2 outline-none bg-zinc-800" type="text" name="title"
                    placeholder="title goes here">
                <textarea class="outline-none block mt-2 px-5 py-2 resize-none bg-zinc-800 w-full rounded-md "
                    name="details" placeholder="Write your task here"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md " value="Create Task">
            </form>

        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(file=> { %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 ">
                        <h1 class="text-white text-3xl tracking-tighter">
                            <%= file %>
                        </h1>
                        <div class="w-full flex justify-between item-center mt-2">
                            <a href="/files/<%= file %>" class="text-blue-500 inline-block">Read More</a>
                            <a class="text-red-400" href="/edit/<%= file %>">
                                Edit Filename
                            </a>
                        </div>

                    </div>
                    <% }) %>
                        <% }else{ %>
                            <!-- don't have file -->

                            <h1 class="text-zinc-600">No task</h1>
                            <% } %>


        </div>
    </div>
</body>

</html>